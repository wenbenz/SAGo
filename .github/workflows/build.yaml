name: Build

on: [ "push", "pull_request" ]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - run: go get -v -t -d
      - run: go build
      - run: go test -v ./...
